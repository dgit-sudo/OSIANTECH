[build]
  command = "npm run build"
  functions = "netlify/functions"
  publish = ".next/standalone/public"

[dev]
  command = "npm run dev"
  port = 3000

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
